{
  "name": "next-forge",
  "version": "5.2.1",
  "bin": {
    "next-forge": "dist/index.js"
  },
  "files": [
    "dist/index.js"
  ],
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "check": "bunx --bun ultracite@latest check",
    "fix": "bunx --bun ultracite@latest fix",
    "test": "turbo test",
    "analyze": "turbo analyze",
    "translate": "turbo translate",
    "boundaries": "turbo boundaries",
    "bump-deps": "bunx --bun npm-check-updates --deep -u -x recharts && bun install",
    "bump-ui": "bunx --bun shadcn@latest add --all --overwrite -c packages/design-system",
    "migrate": "cd packages/database && bunx --bun prisma format && bunx --bun prisma generate && bunx --bun prisma db push",
    "clean": "git clean -xdf node_modules"
  },
  "devDependencies": {
    "@auto-it/first-time-contributor": "^11.3.0",
    "@biomejs/biome": "2.3.15",
    "@repo/typescript-config": "workspace:*",
    "bun-types": "latest",
    "@turbo/gen": "^2.5.8",
    "@types/node": "^24.9.1",
    "tsup": "^8.5.0",
    "turbo": "^2.5.8",
    "typescript": "^5.9.3",
    "ultracite": "6.5.1"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "bun@1.1.43",
  "dependencies": {
    "@clack/prompts": "^0.11.0",
    "commander": "^14.0.2"
  },
  "type": "module",
  "overrides": {
    "parse5": "^7.2.1"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
